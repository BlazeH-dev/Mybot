# P0 准备阶段

> 状态：已完成。当前 fixture 是中立 OfficeCLI 输入，不包含业务周报 DSL。
> 目标：建立后续阶段共用的确定性输入与 CI 回归门，不依赖模型随机输出。

## 固定 Office fixture

目录：`tests/fixtures/runtime_eval/`

- `office_baseline.json` 与 `office_baseline.xlsx` 提供中立 OpenXML hard-gate fixture。
- `tests/skills/test_officecli_runtime.py` 覆盖 OfficeCLI contract、launcher 和运行边界。

固定 `office_baseline` 使用中立 XLSX 与 `total_amount=30` 验证 artifact、OpenXML 和数字一致性。

## GitHub Actions 回归门

`.github/workflows/ci.yml` 在 Python 3.11 下执行：

- `ruff check nanobot/ tests/runtime/`
- Runtime deterministic/cassette/Policy/OCC/HITL/恢复硬门
- OfficeCLI contract、Plan、模型配置与设置 API smoke
- 重生 deterministic Runtime/Subagent 报告并与 `benchmarks/` 已提交快照逐字节比较

全量上游测试仍可能受网络、密钥、端口和 home 目录影响；CI 只圈定确定性子集并持续扩展。

## 验证与出口

```bash
source venv/bin/activate
pytest tests/skills/test_officecli_runtime.py tests/runtime/test_replay_trace_eval.py -q
pytest tests/runtime/ -q
```

- fixture 可重生并被对应 Python Office 库重新打开。
- shared facts 可从中立 XLSX 复算 `total_amount=30`。
- CI workflow 引用的测试与现有文件一致。
- 已提交 deterministic reports 可由同一 fixture 字节级重生。
