# Mybot 修改记录

> 本文件用于记录项目变更。方案、专题教学笔记与代码变更应保持同步。

| 日期         | 修改内容                                                                                                                                                                                                                                                                  |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-14 | 修复候选编辑校验通过但点击“测试所选 Case”又把 9 个继承脚本判为未修改：测试入口统一使用 revision 的原始派生 Skill 干预基线，旧 revision 缺失时按冻结 builtin Skill 自动重建，不再使用父候选 diff 基线。Evidence 加载同时恢复 `frozen-assets-<digest>.json` 中已冻结的本地资产路径并逐文件复核 SHA-256，探针无需重新收集 Evidence、重跑分析或重新下载资产。补充测试前继承干预与冻结资产恢复回归，同步 Skill 自进化教学笔记。 |
| 2026-08-14 | 修复手动 revise 将父候选错误当作干预实现基线，导致继承自上一 revision 的脚本和 `SKILL.md` 规则被判为“未修改”，Editor 被迫重复改写全部类别并消耗大量 iteration：每轮仍以父候选快照计算本轮 diff 和 no-op，但干预 manifest、脚本入口及工作流激活规则统一相对规范化的原始派生 Skill 校验；因此已实现干预可继承，当前 revision 只需处理真实失败反馈或必要 refinement。补充继承干预基线回归并同步 Skill 自进化教学笔记。 |
| 2026-08-14 | 修复 Skill 编辑 Agent 在最后一次 iteration 已完成有效补丁，却因 Runner 返回 `max_iterations` 被控制器直接判失败：`completed` 与无 Runner error 的 `max_iterations` 现在都进入磁盘侧最终校验，只有实际 changed paths、候选安全校验和既有干预校验完成后才形成可审阅 revision；`budget_exceeded`、Provider 错误、取消和其他 stop reason 仍保持失败。补充最后一轮修改后耗尽 iteration 的回归，并同步 Skill 自进化教学笔记。 |
| 2026-08-14 | 修复多原因类别 Skill 编辑被固定 40 次工具调用预算中断且重复校验不可达的问题：编辑前按已选 Evidence 的 Case ID 从本地 benchmark manifest 复制、校验并冻结输入资产到 Task sidecar，不重新收集 Evidence 或重跑分析；缺少本地资产的 `script_required` 类别在 Agent 启动前失败。Agent 通用超长工具结果改写入 revision 独立工作目录，避免 `.nanobot/tool-results` 污染候选且无法删除；工具预算由基础 40 按脚本/工作流类别扩展并封顶 100，11 类别场景为 80，同时保留 20 iteration 上限。修正 `fail_on_tool_error=False` 后仍因历史失败工具事件否决最终候选的冲突，允许 Agent 从 patch 匹配失败等可恢复错误继续，最终由 stop reason 和控制器校验裁决。失败 revision 继续保存 category、目标 Case 和干预契约；保留了候选目录的 `failed/cancelled` revision 可直接作为手动 revise 父候选，避免 Provider 瞬时失败后从头生成。补充资产冻结、动态预算、工作目录隔离、工具错误恢复、失败候选续改和失败元数据回归，同步 Skill 自进化教学笔记。 |
| 2026-08-14 | 修复原因分类超时后仍允许从旧 Finding-only Analysis 启动 Skill 编辑，导致编辑 Agent 反复收到不可修复的“缺少原因类别干预契约”并产生大量 iteration/revision：完成所有批次 Finding 后先持久化分类检查点，分类失败时“重新分析”只复用检查点或当前 Analysis 的既有 Finding，不重新拉取 Evidence、不重跑分批分析；跨批次分类按 `fix_owner` 拆分请求，保留同 owner 全局聚类同时降低单次上下文和超时风险。类别未完成时前后端共同禁止编辑并显示“继续生成原因分类”；编辑 Agent 独立收紧为最多 20 iteration / 40 tool calls。补充分类恢复、owner 分组、编辑前置门和前端状态回归，同步 Skill 自进化教学笔记。 |
| 2026-08-14 | 修复 Skill 自进化点击“重新分析”后看似无响应：Task Store 新增启动新一轮时显式消费历史取消标记的写入语义，重新分析、继续修改和重测不再被旧 `cancel_requested=true` 立即终止；请求等待阶段立即展示重新分析状态，进入证据收集/分析后在活动区持续显示明确说明，并暂时隐藏旧分析与旧候选操作，完成或取消后自动恢复；将候选 revision 按钮改为“基于当前候选继续修改”，避免与重新分析混淆。补充取消后重新分析/修改及旧 revision 前端回归并同步教学笔记。 |
| 2026-08-14 | 将 Skill 自进化从“按 Bad Case 增加文字规则”改为以实际提分为目标的原因类别与可执行干预流水线：分批 Finding 后新增跨批次原因分类，每类绑定 `script_required` / `workflow_required` / `not_skill_repairable` 干预契约，WebUI 按类别多选后仍合并生成一个候选。可程序化类别必须修改 `scripts/`、由 `SKILL.md` 引用准确入口并提供冻结资产只读 JSON 探针；公式、图表、数据质量和 Word 计数补充必需输出约束，探针失败时保留候选审阅但禁止模型重测。Revision 持久化 category/finding/target Case 与干预契约，重测严格使用所选类别覆盖 Case 的去重并集、历史 baseline 和单次运行；仅当全部目标 Case 成功评分且整体平均 delta 大于 0 时推荐，页面展示每类 delta 与“无全局回归验证”提示。手动 revise 可读取候选回答、Judge 原因、工具序列/错误、脚本调用和必需字段覆盖；旧 Finding 任务继续兼容，但旧纯文字候选不能获得可执行干预通过标记。补充后端、ASGI 和前端回归，同步 Trace/Eval 与 Skill 自进化教学笔记。 |
| 2026-08-14 | 修复 PTC 真实多日志任务中的程序重试和串行瓶颈：SDK 明确单次 `run_code` 完成发现/读取/计算/过滤，工具可提供仅用于 PTC 的输出 Schema 并生成嵌套 `TypedDict`；worker 安全注入 `math/json`、单参数 `type()` 和有界 `shape()`，兼容白名单 import，超长 `print` 改为截断而非击穿程序，超大 return 返回结构摘要。并发安全读取在逐次 Policy 检查下继续有界并发；同一 Agent 运行内缓存成功的完全相同只读调用，写入/exclusive 使用 mutation epoch 隔离旧缓存，事件新增 cache hit 与实际子调用指标。DeepSeek V4 Flash 对 8 文件/640 记录任务各跑 3 次：Native/PTC 均 3/3 正确，PTC 中位数从 25.65s/15,354 token/3 轮降至 5.80s/3,777 token/2 轮，三次均为 1 个 `run_code`、9 个实际子调用、0 失败。PTC 教学专题补齐完整题目、固定数据、期望 JSON、修复前失败链、测试配置、六次原始数据、中位数/范围、可比性说明和适用边界；补充 40 项 PTC 定向回归和 105 项 Runtime/WebUI 相关回归，文档仅本地同步，未提交或推送。 |
| 2026-08-14 | 修复 Skill 自进化重测全部 Case 因 Langfuse `ScoreV3_Numeric/ScoreV3_Boolean` 无法写入 progress JSONL 而被误记失败：Benchmark 进度事件统一递归转换 Pydantic Score 为 JSON 原生值，读取端直接提取序列化 Score 的 `value`，每次 Case 重测先清空同编号旧 progress，且 best-effort 进度异常不再击穿已完成的 Experiment Case。同步修复取消重测时 revision 仍停留 `testing`、旧 `cancel_requested` 无法消费而导致再次启动立即取消的问题；历史 `cancelled + revision.testing` 任务可直接恢复重测。补充进度序列化、分数读取、取消落盘和兼容恢复回归，同步 Trace/Eval 与 Skill 自进化教学笔记；取消无效重测后复用原候选重新测试。 |
| 2026-08-14 | WebUI 设置页“系统 → 工具调用”新增 Native / Code / Both 三态切换，复用规范 JSON settings API 持久化 `tools.mode`；切换到 Code/Both 前预检隔离 PTC worker，失败时不落盘。AgentLoop 在新消息开始前只刷新 `mode + ptc` 呈现快照，保存后下一轮直接生效且不重建工具注册表，正在执行的轮次保持原协议，无需重启。补充 settings payload、非法模式、runtime 不可用、热刷新、ASGI 转发和前端交互回归，同步 README 与 PTC 教学笔记。 |
| 2026-08-14 | 修复 Skill 自进化后台分析仍在执行但 WebUI 进度消失：页面不再只依赖 `localStorage` active task ID，同时核对本地指针与后端持久化任务列表并按创建时间恢复更新的 Task，再从 cursor 0 重放活动；指针缺失、陈旧或单任务恢复临时失败均可找回后台任务，临时请求失败不再删除本地指针。补充缺失指针、瞬时失败与陈旧指针三种前端恢复回归，同步 Skill 自进化教学笔记。 |
| 2026-08-14 | 修复 Skill 自进化 Bad Case 分析因模型漏写 Finding 的 `should_modify_skill` 而整批 schema validation failed：该字段明确为 WebUI 默认勾选建议而非 owner/权限判断，Pydantic 缺失时保守补为 `false` 并在输出中保持完整布尔字段；Finding 继续进入人工审阅，`skill`/`mixed` 仍可手动选择，避免不完整模型输出自动触发修改。同步分析提示、schema 回归测试与 Skill 自进化教学笔记。 |
| 2026-08-14 | 将 PTC / Code Mode 补入 Agent Runtime 总方案的能力矩阵、总体架构、全局契约、完整执行链、简历叙事与真实模型评测路线；明确 PTC 是工具调用压缩层而非权限旁路，子调用复用 Runner 治理流水线。为 01-13 每篇教学笔记增加高密度“面试级深挖”，聚焦真相源、核心不变量、设计取舍、故障推演与源码实验；新增 14 全链路融会贯通与 Agent 面试手册，只负责跨模块组合推理，不重复专题正文。同步校准 Skill Evolution 已强制 tested + recommended 应用门、但尚缺固定全量回归的当前边界。 |
| 2026-08-14 | 参考 DeepSeek Harness 收敛 macOS 沙箱：删除 Bubblewrap、多 Provider 选择及受限 curl/DNS `NetworkGrant`，Seatbelt 改为 `(allow default)` + 全局拒绝文件写入，`read-only` 仅开放 `/dev/null`，`workspace-write` 开放规范化 workspace、`/tmp` 和用户临时目录；不限制读取、网络和进程可见性，不再遮蔽 `.git`、凭据或 `.nanobot-runtime`。Exec one-shot、后台命令、持久 Exec Session 与 CLI Apps 统一消费逐调用 `LaunchSpec`，区分 `sandbox-exec:` runner 失败与 `Operation not permitted` 写入拒绝并写入 Trace；非 macOS Restricted 失败关闭，Full Access 保持宿主执行。设置状态新增 file write/read/network 限制事实及 `stdio_mcp`、`officecli_internal`、`gateway` 未覆盖列表，移除 Provider 配置；旧空值/auto/seatbelt 自动迁移，显式 bwrap 告警并禁用受限执行。同步 WebUI 权限说明、测试和安全治理教学笔记，并准确记录沙箱的读写、网络和进程覆盖边界。 |
| 2026-08-13 | 将 Skill 自进化升级为 schema v2 两阶段闭环：先冻结 prompt/gold/checkpoint/Judge/Trace/usage/metrics Evidence，按上下文容量分批调用模型并以 Pydantic Finding 诊断根因，token/延迟只与同模型同 benchmark 高分 Case 中位数比较；用户在 WebUI 审阅 owner/confidence/Evidence/风险后，仅可选择 Skill 或显式 mixed Finding。修改阶段不再接收 LLM 完整文件 JSON，而是复用 `AgentRunner` 与版本固定的内置 `skill-maintainer-v1`，使用仅含七个候选目录文件工具的独立 ToolRegistry，禁止 Shell、网络、MCP、子代理和主 workspace。新增持久化脱敏 `activity.jsonl`、cursor 增量轮询、刷新恢复、取消、Agent 文件活动和分析/编辑/测试三阶段界面；`AgentRunSpec` 不设 evolution 专属 token/tool/timeout 预算，沿用全局迭代、上下文和 Provider 约束。控制器独立重算 diff/digest，并校验路径、符号链接、大小、frontmatter、冻结 manifest/OfficeCLI contract、AST/Ruff、凭据/Case/gold 泄漏及 SkillsLoader；失败候选仅保留审计，普通 apply 收紧为仅允许 tested 且 recommended revision。补充 Fake Provider 多轮编辑、安全路径、Evidence owner、活动、预算和前端 Finding 流程测试，同步 Skill 自进化、Trace、WebUI 与测试教学笔记。 |
| 2026-08-13 | 完善 P9 Skill 自进化工作台：低分 Case 按 `model_preset` 分页签独立展示，切换 DeepSeek/Luna 等评测模型时清空选择和候选，单个任务只接受一个评测模型；取消前后端单轮 20 Case 上限，全选覆盖当前模型全部低分 Case，并以 `source_model_preset + case_ids` 紧凑提交。优化模型改为从已配置 Provider 对应的具名模型 preset 中选择，优先 Sol、不可用时回退首个可用 preset，任务与后续 revision 固定复用所选模型；后端验证 Case 归属与优化模型可用性。补充超过 20 条、跨模型隔离、当前模型提交和优化模型选择回归。 |
| 2026-08-13 | 删除开发前使用、现已失效的 `docs/plans/runtime-steps/` 阶段步骤；将 `docs/runtime-code-notes/` 从 P0-P9 阶段流水账重组为按简历亮点划分的 Agent Runtime 教学指南，覆盖增量架构、Skill 治理、OfficeCLI、Plan/DAG、安全权限、Artifact/Checkpoint、多 Agent、Trace/Eval、Skill 自进化、长任务续跑、WebUI 控制面和测试交付。重写 Runtime 总方案与 AGENTS 维护规则，统一按当前代码/测试说明已实现能力、面试追问、边界和秋招前完善建议，并在 README 增加总方案、教学指南与 Bad Case 分析入口。 |
| 2026-08-13 | 修复评测中心已知 Dataset Run 的定向历史投影不显示分数：本地 Job 的已知 Run ID 优先走定向缓存，不再等待全量 Langfuse 历史扫描；定向读取汇总 Case Judge Score，并按当前 Office release 211 条 manifest 过滤稳定 Run 中早期已排除 Case 0、4、7 的遗留尝试，同时投影 scored/pending score 数量，避免旧尝试污染 DeepSeek/Luna 对比分数。 |
| 2026-08-13 | 修复 Office release 评测全部 Case 已完成后被 Langfuse SDK 内部媒体上传队列收尾超时误标失败：仅当异常明确为 media upload queue timeout，且稳定 Dataset Run 远端回读确认本轮全部预期条目 completed、必需本地分数已存在、无任务失败和同步分数上传错误时继续完成 Job；OTEL 超时、缺失 Case、分数上传错误及其他异常仍失败关闭。断点续跑继续复用原 Job 与已完成 Case。 |
| 2026-08-12 | 完成 P9“评测驱动的 Skill 自进化”首版：新增独立 `#/skill-evolution` 工作台，从 OfficeCLI + OCB 评测中按阈值筛选并手动勾选低分 Case，使用 `gpt-5.6-sol` 基于脱敏 prompt/gold/输出/Judge/trace 证据生成 `officecli-evolved` 候选；支持 `SKILL.md`、scripts、references、assets 的受限修改/新增、多轮 revision、digest/diff 审阅、按原模型仅重测所选 Case、逐 Case score/delta 对比、用户确认应用与一键切回。原 `officecli` 不修改，provider/permissions/evaluator/OCB/Runtime Policy/shared Office Core 冻结；候选测试使用隔离 workspace、独立 checkpoint 与 Langfuse run name。已知 Dataset Run 使用定向后台缓存，避免 P9 冷启动扫描全部历史；前端提供全选/取消全选，前后端统一限制单轮最多 20 个 Case。明确局部重测不构成全量无回退保证，并新增后端/前端回归、真实页面验证与 P9 阶段说明。 |
| 2026-08-12 | 修复双模型历史在运行中被实时事件覆盖：仅实际 `run` Job 投影模型结果，`prepare` Job 不再生成无 Case/Score 的 DeepSeek/Luna 假历史行；worker 的实时 Job 更新缺少 `model_runs` 时，WebUI 保留最近一次历史接口投影，避免两条独立模型记录在 Luna 运行期间消失。 |
| 2026-08-12 | 评测中心历史结果改为按模型独立投影：同一双模型 Job 在运行中即可分别显示 DeepSeek 与 Luna 的状态、完成/失败/剩余 Case、得分、用量和各自 Langfuse Dataset Run 链接；模型行展开时只展示该模型 Case，并过滤已被本地 Job 关联的重复远端行。解决 Luna 必须等整批完成后才出现、双模型结果混在一行的问题，不改变正在运行的评测 checkpoint。同步 P5/P7 阶段说明与回归。 |
| 2026-08-12 | 再次收紧 Office release 正式评测口径：在原固定分层 255 OCB 候选及 41 个失效/受限官方资产排除项基础上，固定追加排除 Case 0、4、7。三条 Case 在模型执行中暴露任务/参考证据契约或稳定产出问题，其中 Case 4 的任务要求与所附单一参考材料不匹配，Case 7 重复无法形成可验收输出，Case 0 按用户确认同样不纳入正式比较；不从 1018 全量池补位，最终固定为每模型 211 Case、DeepSeek Flash 与 GPT-5.6 Luna 合计 422 Case。Prepare manifest、Dataset、fingerprint、评测目录、预估、Job 总数与 WebUI 统一按 44 个排除项和 211/422 口径展示；续跑必须复用已完成的其余 211 条 DeepSeek checkpoint，只执行 Luna 的 211 条。同步 P5/P7 阶段说明与回归。 |
| 2026-08-12 | 修复 Office release licensed prepare 因单个 URL 触发 `curl_cffi` 原生进程异常而中断整批恢复：普通连接先沿用 pinned downloader 的标准 `requests`，仅对明确的 403/503、拦截 HTML 或 TLS 错误使用浏览器指纹；每个缺失资产在独立有界子进程中处理，单文件异常不影响其余缓存继续补齐。原始 URL、Adobe 转换、格式验证、manifest 与 fingerprint 硬门不变；同步 P5/P7 阶段说明与回归。 |
| 2026-08-12 | 修复 Office release licensed prepare 在官方 OCB downloader 访问 Akamai、CloudFront 等来源时集中返回 403/444：外部 benchmark venv 固定加入 `curl_cffi==0.13.0`，运行时仅为 pinned 上游 downloader 的 `requests.get` 注入 Chrome TLS 指纹。保留原始 URL、请求头、Adobe 转换、资产校验和固定 manifest，不改写 benchmark 内容或评分口径。补充依赖锁与 wrapper 合同回归，同步 P5/P7 阶段代码说明。 |
| 2026-08-12 | 扩展 OCB licensed source 兼容恢复：对 403/503、非预期 HTML 和 HTTP/2 传输失败依次使用 Chrome、Safari、Firefox 及 HTTP/1.1 指纹；固定加入 `pypdf==6.15.0`，仅在 PDF 可用空密码正常解密时移除容器加密后再交给 Adobe，页面内容与转换目标不变。真实 404、不可解密文件和全部策略失败仍保持 prepare 硬失败。同步 wrapper 回归与 P5/P7 说明。 |
| 2026-08-11 | 修复本地 Runtime Trace 测试污染仓库：`test_task_cancel` 不再以 `MagicMock` 伪造工作区路径，统一使用 pytest `tmp_path`，避免 TraceHook 将 mock 的字符串表示写入项目根目录；`.gitignore` 排除 nanobot 的本地工作区模板、cron、memory 与 `.nanobot-runtime` trace 状态，防止网关将仓库临时作为 workspace 时污染待提交内容。同步 P5 代码说明。 |
| 2026-08-11 | 取消计划执行完成后的独立 Reflection：`plan complete` 在节点终态和预期产物确定性校验通过后直接完成，不再启动额外 Reviewer、生成 Reflection artifact、自动修订或弹出 reflection decision；同步移除相关 Runtime 状态、checkpoint、trace、WebUI 展示和回归。旧 `reviewing/awaiting_reflection_decision` 计划在下次进入会话或读取计划时自动迁移为 completed，遗留请求只保留反序列化兼容并立即取消。HITL 通用卡片补齐九语言 i18n，审批原因与 recovery 内置问题使用稳定翻译键按应用语言显示，同时继续向后端提交原始协议值。同步更新 P3/P4/P5/P8 方案与代码说明。 |
| 2026-08-11 | 修复 Office release prepare 的整批 Hugging Face snapshot 在正常下载多 GB 许可参考文件时被 10 分钟子进程总时限误判为卡死：保留 10 秒连接、120 秒单次下载超时与 3 次断点续传，将整批 snapshot 的有界总时限扩展为 2 小时，避免 release 数据量与 smoke 共用过短的总时限。Adobe 环境同时自动发现 PATH 内浏览器与 macOS 标准 Edge/Chrome/Chromium 安装路径，使 HTML→PDF→DOCX 许可转换不再依赖手工配置 `CHROMIUM_PATH`；许可源下载和 Adobe SDK 连接将 IPv4 地址排在 IPv6 之前但保留 IPv6 回退，避免本机不可用 IPv6 CloudFront 连接为每个文件额外消耗超时窗口。同步 P5/P7 阶段说明与回归断言。 |
| 2026-08-11 | 修复 Plan DAG 子智能体后台执行与级联中断：主 Runner 不再为空 pending queue 前台等待 child 最多 300 秒；会话仍有 running child 时桌面输入区保留统一停止按钮，`/stop` 并发取消主任务和该 session 全部 child。`maxConcurrentSubagents` 默认值、本机配置和上限统一为 5，direct-child 计数在完成后释放，语义改为“同时最多 5 个”而非任务生命周期累计最多 5 个。active DAG 的 child 节点只允许 PlanScheduler 派发，父 Agent 直接 spawn 被拒；重复 `running` 更新保持幂等，`running + child_id=null` 在 resume 时恢复并重新绑定，避免重复 child 和卡死节点。补充后端 91 项、前端 83 项定向回归并同步总方案与 P8 计划/代码说明；未新增移动端实现。 |
| 2026-08-11 | 修复 Office benchmark prepare 在外部资产源不可达时长期停在 `preparing`：本地 OCB checkout 的 HEAD、工作树和 LICENSE hash 全部匹配时直接复用，不再无条件访问 GitHub；Hugging Face 官方源使用有界连接/下载/子进程超时，默认失败后自动回退 `hf-mirror.com`，显式 `HF_ENDPOINT` 仍优先；补齐官方 OCB downloader 读取 parquet 所需的固定 `pandas==2.3.2`；运行时 wrapper 为 Adobe PDF Services SDK 注入 30 秒连接/120 秒读取超时，并对未生成的转换文件最多重试 3 次，不修改 pinned 上游源码。prepare 新增源码校验、依赖安装、Dataset 下载、manifest、参考文件、Langfuse Dataset、Cloud smoke 和 fingerprint 阶段进度，WebUI 当前任务可显示真实步骤。保留固定 revision、许可证、资产完整性和 Langfuse 硬门。补充缓存复用、依赖、镜像/Adobe 有界恢复与阶段投影回归，同步 P5/P7 阶段说明。 |
| 2026-08-11 | 修复修订计划在卡片点击“执行”后首次确认使用截断 hash：等待确认状态的 Runtime 上下文现在保留完整 `plan_hash`，typed confirmation 恢复到 checkpoint 的 plan tool result 同时携带完整 task/hash 绑定。Agent 可直接以当前 revision 调用 `plan(confirm)`，不再先 hash mismatch、重新 `plan(get)` 后再次确认。补充 Runtime 上下文和 interaction materialization 回归，并同步 P4 代码说明。 |
| 2026-08-11 | 修复 Office smoke readiness 在 prepared 元数据不存在时同时报告“Profile has not been prepared”和“Prepared Dataset contains redacted content”的矛盾提示：未准备时只显示缺失 profile；只有 prepared 文件存在且明确未上传许可内容时才提示重新执行 licensed prepare。补充缺失/已脱敏两种状态回归，并同步 P5/P7 阶段代码说明。 |
| 2026-08-11 | 完成 Plan Mode 单实例卡片与可恢复 child 修复：每个会话只在标题栏和消息 viewport 之间保留一张计划卡片，进度、修订和 Reflection 原位更新；可折叠为单行并按会话跨刷新记忆，子智能体过程继续使用独立桌面侧栏。计划文档收敛为固定 `.nanobot-runtime/artifacts/<task_id>/plan.md` 和 `plan_markdown` artifact，原子替换并绑定当前 revision/hash/checksum；验证后迁移清理旧 `plan-rN.md`/`plan_md_rN`，预览仍按 session 精确授权。Tool 新增可信只读根目录，修复 restricted child 无法读取 builtin Skill；child 单次工具错误改为可恢复迭代，最后仍为 error 时保持节点失败。补充后端、WebUI、迁移、权限与生命周期回归，同步 P3/P4/P8 方案和代码说明；未新增移动端实现或测试。 |
| 2026-08-11 | WebUI 设置页“系统”新增 Langfuse Trace 开关：设置 API 只返回 enabled/configured/base URL，不暴露凭据；关闭后 Runtime 使用本地 JSONL Trace，开启前校验 public/secret key，保存后明确进入可观测性引擎重启状态。补充设置投影、持久化、缺失凭据拒绝、HTTP 路由、API helper、设置页交互和九语言回归。 |
| 2026-08-12 | 修复 Office release 在一个模型的 Case 全部执行后，因 Langfuse 媒体附件后台队列未在 30 秒内清空而中断下一模型的问题：benchmark 最终 flush 现在将 `media upload` 与已有 `score ingestion` 队列超时都投影为可恢复的远端最终一致告警；OTEL trace flush、同步 Score 上传及其他异常仍 fail-closed。现有 Job 可从 Case checkpoint 续跑，不重放已完成模型调用。 |
| 2026-08-12 | 收紧 Agent 空终态恢复提示：无工具 finalization 明确要求直接回答原请求、禁止再调用工具、不得返回空；证据不完整或冲突时必须标注限制并基于显式假设给出最佳支持答案，避免 Office benchmark 已完成分析却因空 final answer 被误记为 Case failed。空响应仍经过有界重试，最终持续为空仍 fail-closed。 |
| 2026-08-11 | 将 Web Search 默认及 WebUI 可选无密钥 provider 切换为 Bing China：正常搜索直接请求 `cn.bing.com`，移除 DDGS 依赖和 DuckDuckGo 请求实现；旧 `duckduckgo` 配置值仅作为迁移别名解析到 Bing，其他 provider 缺少凭据时也统一回退 Bing。 |
| 2026-08-11 | WebUI 右上角新增本轮 Trace 入口：主 Agent 的本地 TraceHook 与 Langfuse observation metadata 均绑定 `webui_turn_id` 和 session；新增受鉴权的只读 trace API，按 session/turn 精确读取本地脱敏 JSONL 或 Langfuse observation，并在 API 投影层二次屏蔽 content/input/output/result/message 与凭据字段。Trace 侧栏实时展示 span、事件、耗时、token、状态及 Langfuse deep link；补充关联隔离、脱敏、HTTP 路由、API helper 和面板回归。 |
| 2026-08-11 | 修复待确认计划无法修改：`awaiting_plan_confirmation` 期间允许普通文字进入受限 plan-only revision 回合，Runtime 撤销旧确认、恢复 checkpoint 并要求 `plan(action="revise")` 生成新 revision；修订不能确认或执行，审批、uncertain recovery 和 Reflection typed interaction 仍保持强阻塞。补充 AgentLoop/PlanTool/Interaction 回归并同步 P3 方案与代码说明。 |
| 2026-08-11 | 收紧 Plan-only 失败语义：只读检查中的 `web_search`、`web_fetch`、网络或 provider 暂不可用不再被模型视为计划创建阻塞条件；必须将其记录为执行风险、前置条件或 fallback，并继续生成结构化计划。实际执行时仍保留工具真实失败与节点恢复语义。补充 execution mode 回归并同步 P3 代码说明。 |
| 2026-08-11 | 增加计划 child 节点执行可见性：SubagentManager 发布绑定 parent task/plan hash/node/child 的 started、iteration、reasoning、tool、checkpoint 和终态 activity；WebSocket 使用独立 `subagent_activity` 事件并写入 transcript，历史接口可聚合恢复。WebUI 将 child activity 保存在独立状态，不混入主 Agent 消息/思考时间线；计划卡片新增“查看子智能体”按钮和 52rem 桌面侧栏，按 child 展示实时思考、工具调用、迭代、耗时、usage、状态与结果，并过滤旧 revision。补充后端、transcript、stream reducer 和计划面板回归，同步总方案及 P5/P8 计划与代码说明；不新增移动端实现或测试。 |
| 2026-08-11 | 修复 active DAG 计划中断/失败后无法继续：PlanTool 新增完整 plan hash 绑定的 `resume`，显式重置 failed 节点、保存 attempt history、清理旧 child/result/error 绑定并重新派发；兼容模型误用 `failed → running/in_progress`。`/stop` 已知取消的 child 改为回到 ready 且不在回调中自行重启，未知副作用仍保持 uncertain 人工恢复。桌面计划卡片在 active 状态始终显示“继续执行”，点击后发送精确 resume 指令，不新增移动端能力。补充 Runtime、child callback 和 WebUI 回归，并同步总方案、P4/P8 计划与代码说明。 |
| 2026-08-10 | 修复计划卡片“查看完整计划”因 Runtime artifact 位于网关默认 workspace、WebUI 会话使用项目 workspace 而被文件预览拒绝：预览接口仅对当前 session `plan_state` 精确绑定的不可变 `plan-rN.md` 增加只读授权，并复核 task/revision/plan hash/artifact id/path/SHA-256；普通外部路径仍返回 403，计划文件被篡改时返回 checksum 错误。补充允许预览与篡改拒绝的 HTTP 回归测试，同步 P3/P4 代码说明。 |
| 2026-08-10 | 将 Plan Mode 升级为 Runtime 持久化 DAG：新增 schema v2、循环/依赖校验、节点状态机、并行 child 调度、增量 revision 与孤立 running 节点恢复；每个 revision 生成不可变 `plan-rN.md` 并登记 checksum/lineage。所有节点完成后强制运行 fresh-context、无工具的独立 Reflection，只有 pass 且无 blocking finding 才能 complete/deliver；needs_revision 最多自动生成两次受影响节点修订草案，之后通过 typed decision 继续、重审或结束。WebUI 桌面计划卡片展示批次、依赖、executor、revision/hash、Reflection 和完整计划预览，不新增移动端实现。补充 Runtime/Subagent/Interaction/WebUI 测试并同步 P3/P4/P5/P8 方案与代码说明。 |
| 2026-07-30 | 优化评测详情 Variant 列：根据 case 数据自动识别真正变化的对比维度，模型对比仅显示 `DeepSeek-v4-flash` 或 `gpt-5.6-luna`，不再重复 `ocb / officecli` 等固定项；后续 Skill 或 benchmark 对比会同样显示对应变化项。补充双模型详情展开回归。 |
| 2026-07-30 | 修复双模型 Office 评测的本地 Job 总记录详情重复：Langfuse 远端 case 投影显式保留 `model_preset`，总记录按 benchmark、Skill、模型和 case id 四元键合并对应 Dataset Run，不再让 Luna 与 DeepSeek 的同一 case 相互覆盖 score、usage 和 trace。补充双模型同 case 的历史详情回归。 |
| 2026-07-30 | 修复 Office benchmark Case 已执行成功且 Langfuse 最终已有分数，却因 Score 异步入库延迟被标记 failed：同步评分上下文内，Langfuse SDK 自己在 `run_experiment()` 返回前触发 flush 时仍提交 Trace/媒体，但不等待与本次同步上传无关的旧后台 Score 队列；Case Score 在回读窗口内暂不可见时保留 `completed` 并将 `score_status` 记为 `pending`；最终 flush 仅对 `score ingestion` 队列超时降级为等待远端最终一致。历史上全部 Case 已完成且唯一主因为 Score 延迟的 Job 读取时兼容投影为 `awaiting_review`。同步 Score 上传错误、Case 失败、OTEL flush 超时和其他异常仍保持 fail-closed。补充旧队列、回读延迟、历史投影与 flush 分类回归。 |
| 2026-07-30 | 修复删除一条评测历史时其他远端结果短暂消失：Langfuse 删除完成后不再清空全量历史缓存，只从各 limit 缓存中剔除实际删除的 Dataset Run，并保留其他行作为 stale-while-revalidate 快照；新增删除 tombstone，防止删除前已启动的并发刷新重新投影旧 Run。补充目标缓存保留与并发刷新回归。 |
| 2026-07-30 | 修复 Office benchmark 在执行期间 Langfuse Annotation Queue 被删除后使用陈旧 ID 导致整项失败：审核项入队遇到 404 时按原队列名重新查询或创建并重试一次；失败分类新增 `langfuse_queue_missing`，优先于更早且已恢复的模型网络超时，确保评测中心展示真实终止原因。补充队列自愈和失败分类回归。 |
| 2026-07-30 | 重新整理 `docs/plans/` 与 `docs/runtime-code-notes/`：plans 统一收敛为当前状态、实施步骤、验收出口和明确不做，删除 P5/P7 的运行流水账、旧方案和历史测试快照；runtime notes 补齐 P1 OfficeCLI、P5 Trace/Eval、P7 评测交付的真实调用链、模块职责、设计取舍、验证命令、边界和面试问答，并校准 P0/P2/P3/P8 的过期表述。未改变代码行为。 |
| 2026-07-30 | 删除旧 Python Office Skill 的全部代码、manifest、schema、fixture 和测试；OfficeCLI 成为唯一 Office Skill。Office OCB benchmark 改为在相同输入、Policy、evaluator 下分别比较 `gpt-5-6-luna` 与 `deepseek-v4-flash`，每个模型使用独立 Dataset Run、workspace、checkpoint 和进度记录。同步更新 P1/P5/P7 阶段计划与代码说明。 |
| 2026-07-30 | 评测中心新增 Case 级重跑：终态本地 Job 的失败或已完成 Case 可从明细行单独重新执行，WebSocket 增加 `evaluation_case_rerun` 控制，复用原 Job/稳定 Langfuse Dataset Run，只过滤目标 benchmark/Skill/Case，并强制绕过远端完成 item 与本地 checkpoint；其他 Case 不重复调用模型。补充 Job/worker/CLI/WebSocket/WebUI 契约测试及 P5/P7 阶段说明。 |
| 2026-07-30 | 修复 Office benchmark Agent 反复全局搜索 Skill 入口：每个 Case 请求显式注入 OfficeCLI launcher/backend/contract 和 Skill 根目录绝对路径，并禁止扫描 `/Users`、`$HOME`、根目录及父目录；避免隔离 workspace 未复制 builtin Skill 资源时触发递归 `find`、超时和额外模型调用。补充 OfficeCLI 路径提示回归。 |
| 2026-07-30 | 评测历史删除改为异步清理：删除接口完成终态校验并登记后台任务后立即返回，WebUI 立即关闭确认框并移除历史行；后台分别删除本地 Job/日志/progress/checkpoint 和 Langfuse Dataset Run/Run Items，云端耗时不再阻塞页面，任一侧失败也不影响另一侧并写入网关日志。远端失败的 Run 后续从 Langfuse 刷新时重新出现，可再次删除。 |
| 2026-07-30 | 彻底删除两套不再使用的 Office benchmark，公开评测收敛为 OCB-only：移除 revision/license/smoke profile、数据下载和 materialize adapter、专用 evaluator、release 样本参数、Resume/Score 特判、WebUI catalog/兼容字段、专用依赖与测试；历史投影只接受当前 OCB Run；`office-smoke` 改为 OCB 4 case × 2 Skill（8 runs），release 只保留 OCB 255/509/1018 三档。同步更新 README、P5/P7 计划和阶段代码说明。 |
| 2026-07-30 | 修复评测历史删除“点击无反应”：嵌入式 `websockets` 16.1 的 HTTP parser 只接受 GET，原 POST 删除请求在路由前就被断开并表现为 `Failed to fetch`；删除确认改为带 `confirm=1`、`no-store` 的显式 GET 路由。本地终态 Job 与 Langfuse 远端清理解耦，先删除本地 Job/日志/progress/checkpoint，Langfuse 清理最多等待 8 秒；远端网络中断、503、凭据或其他异常通过 `remote_error` 返回，不再阻塞本地历史删除。Langfuse-only 历史仍需远端删除成功才移除；前端关闭成功确认框并显示远端失败原因，错误直接显示在删除弹窗内。补充网关与 WebUI 回归测试。 |
| 2026-07-30 | 收紧中转 LLM 错误重试：带结构化 HTTP 状态码的 5xx/429/408/409 等瞬时错误，即使调用方处于 persistent 模式也只执行首请求后的 3 次重试；中转返回的超长 `Retry-After` 限制为 4 秒退避上限，第四次失败响应立即返回，避免 503 或其他网关错误长期占用评测 worker。终止进度文案改为准确报告“重试 3 次”，并补充 502/503 回归。 |
| 2026-07-30 | 补齐 Office Benchmark Resume 的失败传播与历史去重：Case workspace 现在从固定 fixture 幂等重建并清除旧尝试产物；Langfuse SDK 隔离并过滤 item 异常后，CLI 会核对请求数与返回结果，缺失任一 Case 即携带 Case 明细让 Job fail-closed，不再静默继续后续 variant；评测历史按 Dataset item 只保留最新尝试后再计算状态、Score、usage 和指标，消除 Resume 重放造成的重复 Case 与旧失败污染。运行中的旧失败 Case 作为 pending 重试，活跃远端 Run 优先显示 running；Job 终态失败后再展示结构化失败原因。 |
| 2026-07-30 | 评测历史删除扩展为终态结果全覆盖：已完成、失败、取消和中断的本地 Job 均可删除；删除前先通过 Langfuse 官方 `delete_dataset_run` 删除关联 Dataset Run 及其 Run Items，再清理本地 Job、日志、progress 与私有 checkpoint。仅存在于 Langfuse 的远程历史行也支持删除；删除成功后清空历史读取缓存，后续刷新不会再次投影该 Run。运行中的 Job 仍不可删除，远程删除失败时保留本地记录以便重试。 |
| 2026-07-30 | 修复评测历史 Scores 列误展示完整模型输出：Langfuse v3 Scores 会返回 `data_type=CORRECTION`、名称为 `output` 的文本修正记录，旧投影接受字符串值并将其聚合到 Run/Case Score，导致数千字符回答撑高表格。历史接口现在只投影数值/布尔指标，文本修正仍只留在 Langfuse 审核面；WebUI 同时拒绝渲染超过 80 字符的异常字符串 Score，覆盖旧缓存和异常 payload。补充后端与前端回归。 |
| 2026-07-30 | 修复 `office-release` licensed prepare 读取 OCB 全量 manifest 时的 `JSONDecodeError`：上游个别文本含合法 Unicode 行分隔符 `U+2028`，旧 `_read_rows()` 使用 `splitlines()` 将其误判为 JSONL 记录边界。现改为按文件物理换行读取，不改写 benchmark 原文或 fingerprint 口径，并补充 Unicode 行分隔符回归。Adobe PDF Services 凭据增加 macOS Keychain 自动注入：环境变量优先，缺失时从独立 service 或统一 `Mybot Adobe PDF Services` 项读取，只传给官方 downloader 子进程，不写入 config、命令参数、日志、cache 或 prepared 文件。 |
| 2026-07-30 | release prepare 的 Hugging Face 固定 revision 下载显式禁用 Xet，遇到本机 Xet 响应解码失败时退回可断点复用的普通 HTTP 下载；不改变数据、revision、manifest 或 fingerprint。 |
| 2026-07-30 | 整理评测历史操作列：一个本地 Job 关联多个 Langfuse Dataset Run 时，不再平铺多个无法区分的相同外链图标，改为单一 Langfuse 菜单并保留全部子 Run 入口；终态本地 Job 新增删除按钮和二次确认，删除对应 Job JSON、progress、log 及私有 Case checkpoint。删除不会改动 Langfuse Dataset Run/Score 真相源，确认框明确提示该边界；运行中或排队中的 Job 禁止删除。补充 Job Service 与 WebUI 回归。 |
| 2026-07-29 | 扩展评测中心历史指标：Langfuse 投影按每个 Dataset Run/Case 的 generation observation 汇总并展示实际 input/output/total token、缓存 token、模型调用次数、模型延迟和 TTFT；分数仍以 Langfuse Score 为准，usage 不可用时显示空态，不用估算值冒充实际消耗。已关联 Dataset Run 的本地 Job 行同步回填这些指标；补充后端聚合、前端展示和缺失/分页回归。 |
| 2026-07-29 | 修复 `office-smoke` 在首个 4-case variant 完成后永久停在 `4/24`：Langfuse 4.14.1 的内部 `flush()` 会在 score/media consumer 异常退出时无限等待队列。Mybot 现在在 SDK flush 前清理并重建死亡 consumer，对 OTEL flush、score queue 和 media queue 分别实施 30 秒硬超时；超时使 Job 明确失败并保留 Case checkpoint，而不是无限占用单 worker。共享 Langfuse runtime 的临时 Agent 释放时不再逐 Case 强制 flush，最终 owner 仍通过 shutdown 完整提交。补充 consumer 自愈、SDK 内部有界 flush、队列超时和共享 runtime 释放回归。 |
| 2026-07-29 | 为评测中心补齐付费 benchmark 的 Case 级断点续测：取消、失败或 worker 中断后可手动 Resume 同一 Job/同一稳定 Langfuse Dataset Run；远端已成功 item 完全跳过，本地已完成但远端未成功的 Case 从权限 `0600`、带输入指纹的私有 checkpoint 复用模型输出，只重跑 evaluator/Trace，只有无有效 checkpoint 的 Case 再次调用模型。Job 持久化 `resume_count/history`、已完成/剩余/复用 Case，WebSocket 和页面新增 Resume，与“从头 Retry 新 Job”明确区分；网关重启不自动恢复付费调用。补充 Job/worker/CLI/Langfuse/WebSocket/前端测试，并同步总方案、P5/P7 计划和代码说明。 |
| 2026-07-29 | 新增 Mybot 评测中心与可扩展评测框架：侧边栏加入“评测”和 `#/evaluations` 独立页面；新增受信任 Office suite manifest/adapter、共享 catalog、preflight/estimate、持久化单 worker Job 队列、取消/重试/重启中断恢复、HTTP catalog/readiness/history/cases 查询、WebSocket 启动/进度广播；页面展示 benchmark/Skill/model/runtime 选择、token 估算、硬门、队列、Case 状态、分数摘要和 Langfuse Dataset Run/Trace/Annotation Queue 链接，不复制完整 Trace、模型输出或人工评分。Langfuse 历史投影使用后台 stale-while-revalidate 缓存，避免远端慢查询阻塞本地 Job 轮询。同步修正总体方案、P5/P7 计划与代码说明中的旧“无 benchmark WebUI”边界。 |
| 2026-07-24 | P5 benchmark 改为只统计 token：删除 `profiles.json` 的 Luna/Terra 价格字段、`estimated_usd`、`pricing_configured`、`run --confirm-cost` 及价格硬门；`estimate` 现在按 Luna Agent/Terra Judge 的 input/output 分项输出预计 token 和总量，实际 usage 以 Langfuse generation 为准。通过 LibreOffice 官方稳定版渠道安装 Homebrew cask 26.2.4，锁定 `/Applications/LibreOffice.app/Contents/MacOS/soffice`，完整版本为 `LibreOffice 26.2.4.2 0229ac93fcf0d7cbc6376066c6f35021cef002dc`；同步更新 README、P5/P7 计划与阶段说明、benchmark README 和 contract 测试。 |
| 2026-07-24 | 完成 P1.1 OfficeCLI：将旧 `office-automation` 替换为 `officecli`，新增单一 `scripts/office.py` request/result JSON 接口，覆盖 DOCX/XLSX/PPTX 的 inspect/query/create/apply/validate/render、中立 selector、输入哈希只读校验、artifact root/transport path 限制、批量 apply 临时文件全量执行并通过 OpenXML validation 后原子发布、tracked changes/SmartArt/动画/复杂母版显式 unsupported，以及外部 LibreOffice 路径/版本锁。新增精确 constraints、request schema、三格式中立 fixture 与回归；删除旧 report/slide DSL、renderer、周报 fixture/test，将 OfficeCLI contract case 归位，并从 `disabledSkills` 只清理旧 id。P5 Core 离线 eval 与 CI 同步切换到中立 fixture并重生 deterministic report；Runtime suite 为 `64 passed, 1 skipped`。同步更新总方案、P0/P1/P5 阶段计划/代码说明和索引。 |
| 2026-07-23 | 完成 P5.1 Langfuse 接入方案评审与实施步骤细化：确认 Langfuse Python SDK v4.14.1 的 OTel observation、`propagate_attributes`、`mask_otel_spans`、`langfuse.openai` drop-in、`run_experiment`、Dataset/Experiment、SDK evaluator、Custom LLM-as-a-Judge、Code Evaluator、Annotation Queue 等能力与方案假设完全契合。关键改进：1) `observability.langfuse.enabled=false`（默认）时保留现有 JSONL TraceHook 作为本地调试路径，启用后切换到 Langfuse SDK 并停写 JSONL，二者互斥；2) Provider 必须在 `runner._request_model()` 内逐调用创建 generation observation，优先使用 `langfuse.openai` drop-in（从 config 设置环境变量后导入）自动追踪 OpenAI/DeepSeek/GPT-5.6；3) Tool 必须在 `runner._run_tool()` 内逐调用创建 tool observation；4) Config schema 新增 `observability.langfuse.*` 字段；5) 新建 `nanobot/cli/benchmark.py` 封装 `run_experiment()` 和 export。实施步骤按风险与依赖重排为 S0 准备 → S1 Provider drop-in → S2 TraceHook 双模式 → S3 Tool → S4 Generation（非 drop-in） → S5 Subagent → S6 Masking → S7 测试迁移 → S8 Benchmark CLI → S9 Experiment Runner → S10 LLM-as-a-Judge → S11 SDK Evaluator → S12 Annotation Queue。同步更新 P5 方案（observation 映射表增加创建位置列、配置说明、实施步骤表格化）、P5 代码说明（增加 P5.1 核心改动章节）、总方案（4.8 Trace 边界细化）和本修改记录。此次为规划性质，不涉及代码实现。 |
| 2026-07-23 | 收缩冻结与交付范围：P6 Research 通用性验证降为未排期选做，从后续主线、冻结前必做和最终验收移除；P7 删除固定 Demo、视频和演练交付，收敛为 benchmark、一键可复现入口、README 最终结果页、架构/quickstart 和答辩材料。README 只展示当前最终能力、使用方式、指标、证据和已知边界，不再叙述阶段实施、方案演进、改名迁移或历史修改过程。同步更新总方案、P3.1/P6/P7 计划、P0/P4/P5/P6/P7 阶段说明和索引，不涉及代码行为。 |
| 2026-07-23 | 收敛 P5/P7 benchmark 评估口径：官方 evaluator 可运行且严格复现时只采用 `official_score`；官方缺失、不可运行、覆盖不完整或 action space 改变时才使用 Mybot fallback，记录 `mybot_score`；fallback 的 LLM-as-a-Judge 固定为 `gpt-5-6-terra`。P5 Core 的 Runtime hard gate、cassette 和红队保留为独立回归证据，不重复计入官方已覆盖的 benchmark 维度；同步更新总方案、P5/P7 计划和阶段代码说明。 |
| 2026-07-22 | 将“官方 evaluator 还是自有 Judge”写成 P5.1/P7 固定评估契约：有官方 evaluator、golden assertion 或官方 Judge 时固定 revision/依赖/模型/prompt/action space 原样运行并单列 `official_score`，严格复现才标记 `official-comparable`；Mybot hard gate 独立检查文件、数字、OpenXML、Policy、artifact、恢复和安全；`gpt-5-6-sol` 只补官方未覆盖的开放语义、风格和视觉维度，单列 `custom_judge_score`；高风险、hard/Judge 和官方/Judge 分歧进入人工审计。官方不可运行或 adapter 改变 action space 时标记 `compatible-custom-judge`/`Mybot adapter`，不跨 evaluator 合成总分，Langfuse 仅镜像各类结果。此次同步 P5/P7、总方案及阶段代码说明，不实现 evaluator 或 Judge。 |
| 2026-07-22 | 按 Langfuse SDK 的实际责任边界细化 P5.1：保留 Mybot `TraceHook`、事件语义、父子关联、脱敏、本地 JSONL、cassette、确定性 hard gate、Judge 编排和人工审计，将当前“创建事件即 `_append()`”重构规划为带版本的 `TraceEvent -> normalize/redact -> CompositeTraceSink -> JsonlTraceSink + LangfuseTraceSink`。Langfuse sink 仅作为官方 Python SDK 薄适配器，不自研 HTTP/鉴权/批处理/通用重试，并优先复用 SDK observation/batch/flush；新增 Provider 与 Runner 工具执行边界的逐调用 start/end，明确 task/session、Agent/child、generation、tool、Policy/Interaction、artifact/checkpoint 和 evaluator/score 映射，避免只上传粗粒度 Trace 或重复 generation。补充本地 hard gate 不受远程 score 反向影响、实施顺序、幂等历史同步、断网/限流降级、observation 闭合和本地/远程 id 关联验收。此次仅修改 P5.1 计划与 P5 阶段说明，不实现 SDK 接线。 |
| 2026-07-22 | 完成 P3 Sandbox closure：Exec one-shot 与持久 session 不再拼接/二次解析 sandbox shell 字符串，统一执行 `SandboxLauncher` 生成的不可变 `LaunchSpec.argv/cwd/env`；历史 `agent/tools/sandbox.py` 仅保留为委托统一 launcher 的兼容适配器。restricted Shell 强制 non-login、workspace-local `HOME` 和显式最小 `PATH`；Seatbelt/Bubblewrap 只开放必要系统运行时/显式只读根，不再将 Linux 宿主 `/` 整体暴露为只读，并对 `.git` 与 Runtime control metadata 同时隐藏读写。Bubblewrap 会由可信 launcher 先建立缺失的 Runtime control mount point，拒绝 protected symlink，再以空只读挂载遮蔽；真实后台子进程不能越界落盘。Runner 的参数绑定 `NetworkGrant` 已端到端进入 Exec one-shot 并生成 pinned curl argv，CLI Apps 和持久 session 不隐式继承临时 grant；原生 Windows restricted 继续 fail closed，provider 启动期故障返回 `sandbox_start_failed`。新增精确 argv、网络授权隔离、linked-worktree metadata、真实 provider 和兼容入口回归，扩大验证为 `255 passed, 1 skipped`（macOS 跳过 Linux Bubblewrap real smoke）。同步将 P3 标记为已完成；P3.1 Worktree 仍为未排期选做。P3 不覆盖 gateway/channel/预配置 stdio MCP，也不提供容器/microVM 可重建 benchmark 环境。 |
| 2026-07-22 | 确定 Langfuse 部署采用日本区 Langfuse Cloud（东京，`https://jp.cloud.langfuse.com`），不建设本地自托管栈，因此不需要 Redis、MinIO、ClickHouse 或 PostgreSQL。P5/P7 接入仍使用后端 Python SDK `LangfuseTraceSink` 与本地脱敏 JSONL 并行，仓库和 CI 默认关闭远程 sink，个人环境在日本区创建账号、项目与 API Key 后启用；各区域账号/Key/数据完全隔离，不使用默认欧盟入口。新增固定遥测 endpoint、故障降级、历史同步、Cloud smoke、WebUI deep link 和数据出境边界：默认不上传原始 Office 文件、正文、artifact、密钥和个人信息，真实公司或敏感数据在合规审查前保持远程 sink 关闭。此次仅更新 P5/P7、总方案、阶段代码说明和索引，不实现 Langfuse 接线。 |
| 2026-07-22 | 复核并调整 Runtime 沙箱与工作区隔离计划：确认 P3 的 Policy/HITL/OCC、Seatbelt/Bubblewrap provider 已落地，但 Exec one-shot/session 仍经过兼容 sandbox wrapper，未统一消费 `SandboxLauncher` 与 `NetworkGrant`，因此 P3 从“已完成”校准为“待补强”，新增 S6 统一 `LaunchSpec` 和受批直接 curl 端到端验收；一次性网络 grant 只能启动独立 one-shot curl，不能解锁持久 session。restricted workspace 外路径统一明确为 hard deny，只能切换 workspace 或显式选择 Full Access。将 Git worktree 拆为独立 P3.1 选做规划，保留 WebUI 新聊天显式选择、HEAD 基线确认、服务端持久绑定、跨进程 registry lock、中间状态对账、clean fork 与保守清理设计；明确它未排期、未实现，不属于 Runtime Core、冻结前必做或最终验收，只有主线完成且真实冲突证明收益后才启动。此次仅修改计划、阶段说明和索引，未实现 Runtime/WebUI 代码。 |
| 2026-07-20 | 将 `docs/runtime-code-notes/` 的 P0-P8 全部重构为面向项目学习与秋招面试的教学型说明：统一补充阶段前置问题、核心概念、真实调用链、关键代码职责、设计取舍、验证证据、未实现边界、30 秒讲法、高频追问和自测题；新增总学习索引。对照实际代码纠正或收紧易过度表述的边界，包括 restricted workspace 外路径当前 hard deny、P4 不承诺 exactly-once、P6 Research 尚未落地、P7 仅部分完成、P8 child 不自动共享/登记全部父子 artifact 且完整 child checkpoint 调度未实现。此次只修改文档，不改变 Runtime 行为与阶段计划状态。 |
| 2026-07-20 | 调整 P8 Subagent 生命周期：移除 `spawn` 暴露的 `token_budget`、`timeout_seconds`、`max_tool_calls`，child Runner 不再设置 token/tool 配额或总 lifecycle timeout，避免父 Agent 低估工作量导致正常长任务失败。保留用户/父任务取消、网关关闭、单次 LLM timeout、安全边界、最多 5 个 direct child 与 200 轮循环熔断；循环熔断现在以失败状态返回部分进展。同步更新 P8/P5 计划、代码笔记、确定性 metric 与 single/multi 报告字段；完整 Runtime 验证为 `56 passed, 1 skipped`。 |
| 2026-07-20 | 收敛计划确认 UI：pending `plan_confirmation` 仍保留在前端状态中用于 task id/plan hash/revision 绑定恢复，但不再弹出通用 InteractionRequest 卡片；计划模式写完后只显示计划卡片右下角“执行计划”快捷键。 |
| 2026-07-20 | 修复 WebUI 计划模式生成计划后右下角缺少“执行计划”：Runner 先前把已成功创建但挂起等待确认的 plan tool event 输出为无 result error，前端只能保留 `creating`。现保留 plan result `end` 事件，恢复卡片按钮；点击时按 task id + plan hash 直接回复 revision-bound `plan_confirmation`，不再发送会被 pending interaction 拦截的普通聊天。 |
| 2026-07-20 | 修复对话请求在调用 LLM 前报 `Object of type StringSchema is not JSON serializable`：`request_user_input` 的 option `description` 字段曾误命中 `ObjectSchema` 构造参数，现改为显式 `properties` 映射；`ObjectSchema` 对同类非字符串 root description 冲突改为立即 fail-fast。新增工具定义 JSON 序列化回归，并验证 AgentLoop 注册的 20 个工具定义均可序列化。 |
| 2026-07-18 | 完成 P3 Core：新增 macOS Seatbelt / Linux Bubblewrap 真实 provider smoke、统一 SandboxLauncher、restricted 默认断网与 fail-closed；受批网络收敛为直接 curl argv，绑定 command/domain/port/expiry/审批时 DNS 公网 IP 并以 `--resolve` 固定目标。新增确定性 PolicyEngine、受保护路径/SSRF hard deny、参数/plan/child 绑定一次性 approval、三档持久化 InteractionRequest、typed WebSocket/WebUI 卡片与原 tool_call 恢复；plan-only create 进入 `awaiting_plan_confirmation`，普通聊天不能绕过等待。`write_file/edit_file/apply_patch` 对已有文件启用 actor-local SHA-256 OCC，多文件 patch 全量 preflight。 |
| 2026-07-18 | 完成 P4 与 P8 必做：输入附件在 plan create 时快照到 task artifact root，ArtifactStore 支持 metadata/checksum/lineage/child boundary，plan 每次状态更新同步刷新 artifact checksum；仅 active/completed 且 hash 绑定的计划写 durable checkpoint，safe pending 可恢复，uncertain 外部副作用转 `recovery_decision(required)`，新 AgentLoop/SessionManager kill→resume 测试通过。Subagent 新增 active-plan gate、最多 5 个 direct child、禁止嵌套、restricted child artifact root、独立 FileStates、token/time/tool budgets、父路由 question/approval、父子 trace；共享 workspace 文件租约按计划未实现。 |
| 2026-07-18 | 完成 S5.0 与 P5 Core：提交 4 条无网络 cassette；新增脱敏 OTel-shaped JSONL trace、human wait、父子 span 与 OTLP JSON 导出；新增 12 个确定性 metric、5 个固定 case、安全红队、真实 OpenXML ZIP/XML/relationship 校验和截图 visual sanity。CI 安装 Bubblewrap并运行 runtime suite/报告生成，提交 `benchmarks/latest.*`、`subagent-comparison.*` 与 `docs/plans/metrics-baseline.md`；报告生成器按排序 fixture 计算 digest、移除时钟漂移，CI 对已提交报告做字节级重生比较。本地验证为 runtime `52 passed, 1 skipped`、受影响后端 `236 passed`、WebUI 相关 `51 passed` 且 lint/生产构建成功；全量历史套件为后端 `4162 passed, 19 failed, 5 skipped`、WebUI `406 passed, 2 failed`，失败均集中于本阶段外的 provider/facade 和旧前端基线，未纳入本次 Runtime Core 出口。 |
| 2026-07-18 | Runtime 收尾清理：TraceHook 不再把非 `Path` 测试 double 解析成仓库根伪路径；Subagent 遇到旧测试的 mock workspace 时使用自动清理的系统临时目录，生产 `Path` artifact 边界不变。补充回归防止再生成 `MagicMock/` 临时目录。 |
| 2026-07-16 | Skill 设置页新增启用/禁用操作，继续只写入 `agents.defaults.disabledSkills`，但通过运行时 control 热刷新主 Agent 与子代理，因此不需要重启网关，且只从下一轮对话生效。输入框 `@` 候选新增可用 Skill；选中的 `@skill-name` 以结构化消息字段传入，运行时复核可用性后加载其 `SKILL.md` 作为本轮强制路由上下文，并隐藏自动选择摘要；未指定时保留模型自主选择。补充 API、WebSocket、Context、热刷新、前端设置及定向回归。 |
| 2026-07-16 | WebUI 对话框附件由仅图片扩展为受限通用文件上传：支持 PDF、DOCX、XLSX、PPTX、文本、配置及常见源码文件，浏览器读取后以 WebSocket 附件发送；网关按 MIME/扩展名 allowlist、单文件 20MB、单轮 10 个文件和总计 24MB 校验并保留安全的原文件名扩展。文档正文会提取进模型上下文，同时保留受控媒体目录中的附件路径，使 Agent 能对同一上传副本进行读取和编辑；前端以文件卡片展示非图片附件，并补充前后端回归、lint 与构建验证。 |
| 2026-07-16 | 参考 Codex 重构 P3 安全方案：明确 `sandbox_mode`、`approval_policy`、`approvals_reviewer` 是三个正交控制，“替我审批”仅是后续 `auto_review` reviewer，不等于沙箱也不授予权限。P3 必做新增 Default Permission 下 OS sandbox：plan-only/read-only、Default/workspace-write、Full Access/danger-full-access；macOS Seatbelt、Linux/WSL2 Bubblewrap、restricted provider 不可用时 fail closed，命令默认断网并以 command/root/domain/expiry 参数绑定最小 escalation。规划统一 SandboxLauncher 覆盖 Exec/session、CLI Apps、OfficeCLI/Skill helper 与 Subagent，明确 MCP/channel/gateway 首版未覆盖；补充 S0-S5 实施步骤、平台 smoke、文件/网络逃逸、无静默降级和审批恢复验收，同步总方案、P3 计划与阶段说明。 |
| 2026-07-16 | 完成 P2 Skill Pack Manifest 与开关：新增可选 typed `skill.yaml` schema，workspace 整包覆盖 builtin，manifest 存在但坏 YAML/schema/name/路径/contract 时局部 fail closed；统一输出 enabled/valid/available/status 与结构化原因，disabled/invalid/unavailable Skill 不进入 Agent summary、显式上下文或 `/skill`，但在 WebUI catalog 中保留诊断。为 `office-automation`、`officecli` 增加随包 manifest，OfficeCLI 仍只引用唯一固定版本 provider contract；permissions 仅声明和展示，不接入授权决策。补充 loader、API、命令、HTTP、前端展示、路径逃逸和兼容性回归，并同步 P2 计划、阶段说明及总方案状态。 |
| 2026-07-16 | 修复 OfficeCLI “Skill 已集成但 binary 未交付”问题：新增随 `nanobot-ai` 安装的 `officecli` console launcher，首次调用按唯一 provider contract 自动选择平台并下载 v1.0.135，强制 SHA-256 校验、文件锁、原子缓存、篡改/自更新恢复和 `OFFICECLI_SKIP_UPDATE=1`；不使用上游 latest 安装脚本。缓存路径在 OfficeCLI 临时 HOME 隔离外保持稳定，SkillLoader 也会检查当前 Python scripts 目录，支持直接运行 `venv/bin/nanobot`；兼容渲染 helper 关闭自动 resident，避免 screenshot 的浏览器子进程拖住命令。补充跨平台映射、缓存复用、checksum fail-closed、隔离缓存、禁更和子进程回归测试，更新 README、第三方声明、总方案及 P1/P2 Runtime 代码说明。 |
| 2026-07-16 | WebUI 浏览器模式的项目选择改为可逐层浏览本机目录：新增受短期 API token 与 localhost 限制保护的目录列表接口，只返回当前目录的子文件夹（不返回文件名或内容），支持进入子目录、返回上级、选取当前目录，并保留手动输入作为兜底；原生桌面壳继续使用系统文件夹选择器。补充目录过滤、无效路径、鉴权与前端 API/i18n 回归。 |
| 2026-07-16 | 修复 Provider 配置后的模型选择：当当前默认模型的 Provider 未配置、用户刚保存新的可用 Provider 时，自动切换到该 Provider 的首个内置预设（OpenAI 为 GPT-5.6 Sol）；模型列表只展示已配置且可聊天 Provider 对应的预设，避免配置 GPT 后仍显示未配置的 DeepSeek。补充后端自动切换和前端过滤回归。 |
| 2026-07-16 | 校准 P3 Policy/HITL/OCC 方案与现有 WebUI 权限能力的边界：确认 Default Permission / Full Access 已是会话级 `WorkspaceScope`（项目目录限制/解除限制），并非参数级工具审批。P3 改为复用现有 workspace access、路径/SSRF/Shell hard boundary 与 FileStates，不再平行重建权限或 sandbox；明确 Default 下本地已有文件写入和高风险 Shell 需 ask、Full 下可作为本地预授权 allow，但两种模式均不绕过 OCC/hard deny，消息、邮件、远程写等外部副作用仍需参数绑定审批。同步更新总方案、P3 实施计划与 P3 阶段说明。 |
| 2026-07-16 | 修复首次启动的 WebUI 模型设置体验：没有可用聊天 Provider 时，隐藏内置模型预设及新增/编辑入口，只展示“先配置 Provider”的引导并直接保留 Provider 配置列表；避免用户在无 Provider 下打开无法保存的模型弹窗。模型弹窗改为等宽响应式双列，模型与 Provider 选择器均填满所在列，消除固定宽度组合导致的右侧越界。补充无 Provider 引导及弹窗布局回归。 |
| 2026-07-16 | 网关启动改为容忍未配置 API Key：需凭据的默认/已选 Provider 缺 Key 时使用无网络、无工具调用的占位 Provider 启动 WebUI；聊天会引导用户前往“设置 → Providers”完成配置。设置页保存 Key 后，下一条新消息沿用既有运行时快照刷新机制自动切换为真实 Provider，无需重启网关。非凭据配置错误继续阻止启动；显式模型前缀优先于遗留默认 Provider，桌面旧 bootstrap 配置可在内置 presets 存在时正确清理。新增 DeepSeek/OpenAI、严格校验、非凭据错误与前端保存后热加载的回归测试。同步更新 P0 代码说明。 |
| 2026-07-16 | 将 MiMo V2.5 Pro/V2.5 LLM 内置预设替换为中转站 GPT-5.6 Sol/Terra/Luna，保留 Xiaomi MiMo 语音转写 Provider；配置迁移仅移除未修改的旧内置预设。WebUI 模型设置改为显式配置列表，支持新增、编辑、立即切换和删除自定义配置，输入框模型菜单增加“管理模型”入口。本机配置清理旧 `gpt-5-6` 并激活 Terra；同步更新测试、README、AGENTS、P0 代码说明与 P5 多模型计划。 |
| 2026-07-16 | WebUI 输入框新增单轮“计划”模式：通过结构化 `execution_mode` 让本轮仅开放 plan 与只读检查工具，禁止写文件、Shell、CLI、MCP 等副作用；plan-only 计划保持待确认且同回合不能执行。普通 WebUI 复杂任务创建 plan 后自动进入 active，可边执行边更新步骤。新增可从实时/历史 tool event 恢复的计划步骤卡片，展示 pending/in-progress/done/skipped，并提供绑定 task id 与 plan hash 的“执行计划”按钮；补充后端协议、执行边界、Plan Tool、前端/i18n 与回归测试。同步更新总方案、P1/P3/P4/P5/P7/P8 实施计划、P1/P3/P4/P5/P7/P8 代码说明与阶段索引，统一“自动激活或显式确认后 hash 绑定，高风险工具仍独立审批”的契约。 |
| 2026-07-16 | 精简 Runtime 总方案与 P0-P8 执行计划：总方案由历史叙事/实现细节混排改为“当前决策、文档权威顺序、全局契约、阶段路线图、cutline、硬门和最终验收”，各阶段统一收敛为实施边界、测试与出口；总行数由 2058 行降至约 902 行。保留双 Office Skill、verified facts、静态 plan、三档 InteractionRequest、policy/hard boundary、文件 OCC、artifact/checkpoint、Subagent 治理、P5 Core/选做边界、时间窗口、指标和 demo，不改变已完成 P0/P1 代码状态。同步更新 P0/P1/P3/P4/P8 说明并补齐 P2/P5/P6/P7“仅规划、未落地”阶段说明，明确后续 AI 按总方案→阶段计划→代码说明→修改记录的顺序执行，必做出口完成前不得扩展选做项。 |
| 2026-07-15 | 将 P3 HITL 从单一持久化 approval 扩展为通用 `InteractionRequest`，明确 `required`、`auto_resolve`、`expire_and_deny` 三档等待策略：等待时当前 LLM 调用结束且 token 消耗为 0，task/turn 保持 `awaiting_*`，由回答或 deadline 原子恢复同一执行链；高风险审批超时只会过期/拒绝，永不自动放行。同步更新总体方案、P3/P4/P5/P7 实施计划、P1 后续接口说明和 P3/P4 阶段规划笔记。P8 保留数量、权限、预算、上下文、artifact 隔离、OCC fail-safe 与父子 trace 为必做，将共享 workspace 文件租约、等待队列、层级锁和冲突可视化降为选做，并新增 P8 规划边界说明。 |
| 2026-07-15 | 按要求将 `docs/` 正式纳入 Git 版本控制并随代码推送远程；移除 `.gitignore` 中对 `docs/`、`docs/plans/` 的排除，继续使用全局 `.DS_Store` 规则过滤 macOS 元数据。后续方案、P0-P8 runtime code notes 与代码变更在同一次提交中同步。 |
| 2026-07-14 | 根据 grill-me 共识同步重构总方案与 P1-P8：Office 能力拆为独立 `office-automation` Python Skill 和独立 `officecli` Skill，默认通用 Office 请求优先 OfficeCLI，两者可分别启用/禁用，共享确定性输入检查、verified facts 与 Runtime 治理但不强制共享 DSL；OfficeCLI 保留完整能力面，由后续 Policy 参数级分类。代码侧抽取 `_shared/office_core`，迁移 OfficeCLI contract/compiler/backend/render helper，恢复 Python-only renderer，补齐 Skill 可用性/独立开关/PPT 页数约束测试，并重写 P1 runtime code note。P8 提升为主线：最多 5 个直接子 Agent、禁止嵌套、权限只收紧、隔离上下文和 artifact、父 Agent 负责事实与汇总，强制父子 trace 及单 Agent/双 Agent 成本时长对比。长期要求已写入 `AGENTS.md`：P0-P8 相关代码或方案变化必须同步更新对应 runtime code notes 与本修改记录。 |
| 2026-07-12 | 将 PilotDeck 调研中最适合 Mybot 主线的两项工程实践写入总体方案与详细实施步骤：P3 新增文件并发写保护，复用现有 `FileStates`，以 `mtime + SHA-256`、expected-absent、全量 preflight 和逐文件原子替换把 read-before-edit warning 升级为 `file_conflict` 硬门；P4 新增白盒记忆治理，在 `MemoryStore`、Dream、GitStore 上增加结构化 memory entry、来源 session/turn/artifact、scope、pin/deprecate、召回 trace、revision/change set 与可逆回滚。同步补充 P5 确定性指标、smoke case、时间线、cutline、面试叙事和最终验收。 |
| 2026-07-12 | P5 Eval Harness 增加 `LLM-as-a-Verifier` 离线轨迹评测 PoC，明确为 S5.4 机动加分项：仅在确定性 eval、trace 和红队硬门完成后试验 compare/track/受控 select，以自有 case 上的相关性、误判、成本和时延决定去留；不进入默认 Runtime，不接在线 ProgressTracker/TurboAgent，不用软评分覆盖数字、权限、文件与 OpenXML 硬校验。同步更新整合方案、P5 详细实施步骤、时间线与 cutline。 |
| 2026-07-12 | 明确个人项目 Git 工作流：默认直接在 `main` 分支开发、提交并推送；除非用户明确要求，不再创建功能分支或 Pull Request。该长期约定已同步写入 `AGENTS.md`。 |
| 2026-07-11 | 计划模式改为静态注册内建 `plan` 工具：固定 schema 提供 create/get/confirm/update_step/complete，计划原子落盘并同步 session metadata，使用 contract hash 绑定确认、依赖检查和最终 artifact 核对；动态计划摘要仅追加到用户消息尾部 Runtime Context，复用 ToolRegistry 稳定排序/definitions cache，避免动态工具或完整计划破坏 system/tool 前缀缓存。新增 Plan Tool 定向测试并同步修订方案、P1/P3/P5/P7 实施步骤与 P1 runtime code notes。 |
| 2026-07-11 | 重构 P1 Office Skill：默认 docx/pptx 后端迁移到固定版本 OfficeCLI 1.0.135，新增 DSL→受控 add/set batch 编译器、严格 binary/版本检查、关闭自动更新、隔离运行环境、`--stop-on-error`、OpenXML validate、batch/validation/run sidecar 与 preview PNG；保留显式 `--backend python` 作为 legacy 差分回归路径，并补充纯编译及真实 binary 集成测试。同步修订总方案和 P1-P8 执行计划：P2 manifest 提升为必做，P5 收缩为 Core，P6 Research 前移，P8 降为机动项并禁止用多 Agent 替代普通确定性并发。 |
| 2026-07-09 | 合并 `Mybot通用AgentRuntime与办公自动化SkillPack项目方案.md` 与 `2026-06-16-agent-runtime增量开发计划.md` 的关键信息，新增 `docs/plans/Mybot通用AgentRuntime与办公自动化SkillPack整合方案.md`；按要求移除详细步骤文档索引与各阶段逐步清单，只保留定位、架构、面试叙事、阶段优先级、时间线、cutline 与验收标准                                                      |
| 2026-07-09 | 新增 `docs/runtime-code-notes/` 阶段代码变更说明目录，先补齐 P0 准备阶段与 P1 Office 垂直切片的代码落点、设计原因、实现路径、验证方式和后续影响说明，并保留 P2-P8 索引占位                                                                                                                                                        |
| 2026-07-07 | 补齐 P1 前端展示：新增 WebUI Office artifact 面板，自动识别 assistant 回复中的 `.nanobot-runtime/artifacts/<task>/` 产物路径，按 plan / quality report / docx / pptx / facts 等分组展示并复用文件预览入口；新增前端单测覆盖解析、点击预览和 MessageBubble 集成                                                                   |
| 2026-07-07 | 开始执行 P1 Office Skill 垂直切片：新增 `nanobot/skills/office-automation/` skill 入口、工作簿摘要 / verified facts / DSL+plan 校验 / docx+pptx 渲染脚本、schema 与示例 metric spec，并新增固定 DSL/plan fixture 和 `tests/skills/test_office_weekly.py` 确定性回归                                            |
| 2026-07-07 | 开始执行 P0 准备阶段：新增 `tests/fixtures/office_weekly/` 固定 Office 周报 fixture（sales_data.xlsx 生成脚本、会议纪要、期望指标、约束、自检测试），并将 GitHub Actions CI 收敛为 Python 3.11 下的 `ruff check nanobot/` + 确定性 pytest smoke 回归门；本地全量 pytest 摸底记录为 `142 failed, 3910 passed, 3 skipped`，后续逐步扩大 CI 范围 |
| 2026-06-16 | 改进并扩展 Mybot 通用 Agent Runtime 与办公自动化 Skill Pack 方案，在适配当前 nanobot 二开架构的同时保留完整工程最佳实践                                                                                                                                                                                     |
| 2026-06-11 | 前端对话框右下角模型标识新增模型预设下拉，选择后通过 `/model <preset>` 切换                                                                                                                                                                                                                       |
| 2026-06-11 | 新增内置模型预设：DeepSeek V4 Pro/Flash、Xiaomi MiMo V2.5 Pro/V2.5，并更新模型切换说明                                                                                                                                                                                                    |
| 2026-06-10 | 初始化：精简通道为仅 WebSocket，配置 DeepSeek，删除 Docker/文档/示例                                                                                                                                                                                                                      |
| 2026-08-14 | WebUI 网关迁移到 FastAPI/Uvicorn ASGI：同一端口继续提供聊天 WebSocket、JSON HTTP API、静态 WebUI 和 Skill Evolution cursor 活动查询；聊天 envelope、token/bootstrap、TCP/Unix Socket、TLS、消息大小与 ping 配置保持兼容。新增规范 JSON 写接口，前端不再把 finding、配置或 API key 放入 URL；旧 query 写接口保留弃用响应头和短期兼容。新增 ASGI、JSON 校验、body 限制、握手状态码、SPA/文档关闭和 WebSocket 集成回归。 |
| 2026-08-14 | 新增 PTC / Code Mode 程序化工具调用：`tools.mode` 支持默认 `native`、只暴露 `run_code` 的 `code` 和灰度用 `both`；根据当前工具 Schema 生成稳定 Python SDK，模型 async 函数体在独立最小环境子进程中执行，通过 JSON Lines RPC 回到 Runner，并复用参数校验、Policy、HITL、workspace、网络和工具执行流水线。子调用按 `concurrency_safe`/exclusive 有界调度，中间结果不进入模型历史，父子事件实时进入 WebSocket/Trace；增加 CPU/墙钟/输出限制、严格 lossless JSON、排队及活跃调用取消、进程组终止和当前会话 sandbox 模式继承，明确这是 containment 而非多租户安全边界。新增 PTC SDK/runtime/Runner、确定性对比评测、配置、原生回归和 WebUI activity 测试，并新增 Runtime 教学专题。 |
| 2026-08-14 | 修复候选曾经探针失败后前端永久禁用“测试所选 Case”的问题：历史失败结果继续展示，但可在验证逻辑、冻结资产或候选修复后对同一个 revision 重新发起测试；后端每次仍重新执行安全检查和全部干预探针，未通过时不进入模型重测。补充原 revision 重试前端回归并同步 Skill 自进化教学笔记。 |
| 2026-08-14 | 新增 `docs/Skill自进化-r18重测报告.md`，记录 27 个目标 Case、24 个确定性探针、平均 `delta +0.201383`、10 提升/16 持平/1 下降的真实运行结果；按“结构化证据不完整”和“字段完整但语义选择/最终回答错误”解释未提分原因，单独记录 Case 117 回归及 c10 完成门方向。明确当前支持从 `tested/test_failed` revision 手动 revise 并复用未提升/失败 Case 的完整反馈，但尚不支持按重测后低于阈值的 Case 逐条选择，`delta > 0` 但仍低分的 Case 当前不会进入失败反馈。同步修正教学笔记中的探针重试描述并补充 post-test 迭代边界。 |
