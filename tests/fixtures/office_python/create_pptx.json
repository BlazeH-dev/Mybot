{
  "schema_version": 1,
  "operation": "create",
  "format": "pptx",
  "output_artifact": {"path": "${ARTIFACT_ROOT}/sample.pptx"},
  "selector": {"kind": "presentation"},
  "payload": {
    "slides": [
      {"layout": "title", "title": "Neutral Deck"},
      {
        "layout": "title_and_content",
        "title": "Draft slide",
        "body": ["First point", "Second point"]
      }
    ]
  },
  "options": {"artifact_root": "${ARTIFACT_ROOT}"}
}
